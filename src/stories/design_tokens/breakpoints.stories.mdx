import { Meta } from '@storybook/addon-docs'

<Meta title="Design Tokens (Settings)/Breakpoints" />

# Breakpoints

Currently we use 1 breakpoint. We distinguish between phone (0 - 450px) and desktop (451px +).
