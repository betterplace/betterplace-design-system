import React from 'react'
import { IconProps } from './types'
import styles from './icon.module.css'

function Icon({}: IconProps) {
  return <div className={styles.iconMain}>This is as Icon stub</div>
}

export default Icon
