export type <%=propsName%> = {
  <%props.forEach(function(prop) {%><%=prop.name%><%=prop.required ? '' : '?'%>: <%-[...(prop.values || []).map(v => `'${v}'`), ...(prop.type || [])].join(' | ') || 'any'%>; <%}) %>
}

